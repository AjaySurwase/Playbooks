- hosts: all
  become: true
  tasks:
    - name: Install nginx
      yum:
        name: nginx
        state: present
        become: true
        notify: Start nginx
  handlers:
    - name: Start nginx
      become: true
      yum:
        service: nginx
        state: started
